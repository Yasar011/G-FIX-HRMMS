import { PlaceholderModule } from "@/components/PlaceholderModule";

export default function PhotographyPage() {
  return (
    <PlaceholderModule
      title="Photography Manager"
      description="Upload albums with categories, EXIF data, location, camera and lens info, tags, and featured photos."
    />
  );
}
