import { PlaceholderModule } from "@/components/PlaceholderModule";

export default function FactoryLayoutPage() {
  return (
    <PlaceholderModule
      title="Factory Layout Editor"
      description="Visually move departments, resize rooms, and place machines, lights, and cameras without touching code."
    />
  );
}
