import { PlaceholderModule } from "@/components/PlaceholderModule";

export default function AiKnowledgePage() {
  return (
    <PlaceholderModule
      title="AI Knowledge Center"
      description="Upload documents to extract text, OCR images, chunk content, and build embeddings for the AI chatbot's knowledge base."
    />
  );
}
