import { PlaceholderModule } from "@/components/PlaceholderModule";

export default function BackupPage() {
  return (
    <PlaceholderModule
      title="Backup System"
      description="Daily backups, restore, export/import the database, version history, and rollback."
    />
  );
}
