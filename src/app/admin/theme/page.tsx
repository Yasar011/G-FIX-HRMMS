import { PlaceholderModule } from "@/components/PlaceholderModule";

export default function ThemePage() {
  return (
    <PlaceholderModule
      title="Theme Builder"
      description="Customize colors, fonts, animations, department colors, dark mode, logo, and loader animation."
    />
  );
}
