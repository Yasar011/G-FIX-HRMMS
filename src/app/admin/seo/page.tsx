import { PlaceholderModule } from "@/components/PlaceholderModule";

export default function SeoPage() {
  return (
    <PlaceholderModule
      title="SEO Manager"
      description="Meta titles, descriptions, Open Graph, Twitter cards, structured data, sitemap, and robots.txt."
    />
  );
}
