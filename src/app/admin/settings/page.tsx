import { PlaceholderModule } from "@/components/PlaceholderModule";

export default function SettingsPage() {
  return (
    <PlaceholderModule
      title="Settings"
      description="Website name, domain, social links, resume, contact info, and legal pages."
    />
  );
}
