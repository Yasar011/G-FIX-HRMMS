import { PlaceholderModule } from "@/components/PlaceholderModule";

export default function AiChatbotPage() {
  return (
    <PlaceholderModule
      title="AI Chatbot Manager"
      description="Configure the Grok API key, system prompt, temperature, blocked topics, and review chat analytics."
    />
  );
}
