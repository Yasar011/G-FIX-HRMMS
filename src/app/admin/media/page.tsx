import { PlaceholderModule } from "@/components/PlaceholderModule";

export default function MediaPage() {
  return (
    <PlaceholderModule
      title="Media Manager"
      description="Central library for images, videos, PDFs, documents, and 3D models with drag-and-drop upload and compression."
    />
  );
}
