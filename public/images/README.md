Drop these files in here (exact names) and push to GitHub — the homepage will pick them up automatically, no admin panel or code changes needed:

- `profile.jpg` — your headshot, shown in the Hero section
- `brandix.jpg` — photo for the Brandix internship entry
- `arvind.jpg` — photo for the Arvind internship entry
- `resume.pdf` — put this directly in `public/` (not here) so the "Download Resume" button works, i.e. `public/resume.pdf`

If a file is missing, that spot just shows a gray placeholder box — nothing breaks.
